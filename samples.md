# Sample Follow-up Questions — Task C9

## Example 1
Input: "I used Redis for caching."
Output: "How did you handle cache invalidation when the underlying data changed?"

## Example 2
Input: "I built a REST API using FastAPI and PostgreSQL."
Output: "How did you optimize your PostgreSQL queries for performance?"

## Example 3
Input: "I used a hash map for O(1) lookup in my solution."
Output: "How did you handle collisions in your hash map implementation?"

## Example 4
Input: "I used indexing to speed up database queries."
Output: "What type of index did you use, and why was it suitable for your use case?"

## Example 5
Input: "I trained a Random Forest model for classification."
Output: "How did you tune the hyperparameters of your Random Forest model?"